[package]
name = "tsvm-parser"
version = "0.1.0"
description = "Parser for the TypeScript-native browser runtime"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
tsvm-ast = { path = "../ast" }
tsvm-lexer = { path = "../lexer" }

[lib]
name = "tsvm_parser"
path = "src/lib.rs"

[lints]
workspace = true

